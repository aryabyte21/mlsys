[project]
name = "chat-engine"
version = "0.1.0"
description = "Optimized LLM serving engine for Track 2: Customer Chat"
requires-python = ">=3.12, <3.14"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "vllm",
]

[tool.uv.pip]
torch-backend = "auto"
