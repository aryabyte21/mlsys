[project]
name = "benchmark"
version = "0.1.0"
description = "Benchmark suite for course project"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "click",
    "tqdm",
    "aiohttp",
    "numpy",
]